<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.title %> | <%= config.title %></title>
    <script src="<%- url_for('/js/tailwindcss3.4.16.js')%>"></script>
    <link rel="stylesheet" href="<%- url_for('/css/font-awesome.min.css')%>">
    <link rel="stylesheet" href="<%- url_for('/css/css2.css?family=Inter:wght@300;400;500;600;700&display=swap')%>">
    
    <!-- Tailwind配置 -->
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#2C5F2D',
                        secondary: '#2C5F2D',
                        dark: '#1E293B',
                        light: '#F8FAFC'
                    },
                    fontFamily: {
                        inter: ['Inter', 'sans-serif'],
                    },
                }
            }
        }
    </script>
    
    <style type="text/tailwindcss">
        @layer utilities {
            .content-auto {
                content-visibility: auto;
            }
            .text-balance {
                text-wrap: balance;
            }
            .animate-float {
                animation: float 3s ease-in-out infinite;
            }
            .transition-all-300 {
                transition: all 0.3s ease;
            }
            .scrollbar-hide::-webkit-scrollbar {
                display: none;
            }
            .card-hover {
                @apply transition-all duration-300 hover:shadow-lg hover:-translate-y-1;
            }
        }
        
        @keyframes float {
            0% { transform: translateY(0px); } 
            50% { transform: translateY(-10px); } 
            100% { transform: translateY(0px); } 
        }
    </style>
</head>
<body class="font-inter bg-gray-50 text-dark">
    <!-- 导航栏 -->
    <header class="fixed top-0 left-0 right-0 z-50 transition-all duration-300" id="navbar">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center py-4">

                <!-- 桌面导航 -->
                <nav class="hidden md:flex space-x-8">
                    <a href="<%- url_for('/') %>" class="font-medium text-primary hover:text-primary/80 transition-colors">首页</a>
                    <a href="#categories" class="font-medium text-gray-700 hover:text-primary transition-colors">分类</a>
                    <a href="#tags" class="font-medium text-gray-700 hover:text-primary transition-colors">标签</a>
                    <a href="#about" class="font-medium text-gray-700 hover:text-primary transition-colors">关于</a>
                </nav>
                
                <!-- 搜索和菜单按钮 -->
                <div class="flex items-center space-x-4">
                    <button class="text-gray-700 hover:text-primary transition-colors">
                        <i class="fa fa-search text-lg"></i>
                    </button>
                    <button class="md:hidden text-gray-700 hover:text-primary transition-colors" id="menuBtn">
                        <i class="fa fa-bars text-lg"></i>
                    </button>
                </div>
            </div>
        </div>
        
        <!-- 移动端导航菜单 -->
        <div class="md:hidden hidden bg-white shadow-lg absolute w-full" id="mobileMenu">
            <div class="container mx-auto px-4 py-4 flex flex-col space-y-4">
                <a href="<%- url_for('/') %>" class="font-medium text-primary py-2 border-b border-gray-100">首页</a>
                <a href="#categories" class="font-medium text-gray-700 py-2 border-b border-gray-100">分类</a>
                <a href="#tags" class="font-medium text-gray-700 py-2 border-b border-gray-100">标签</a>
                <a href="#about" class="font-medium text-gray-700 py-2">关于</a>
            </div>
        </div>
    </header>

    <!-- 英雄区域 -->
    <section class="min-h-screen flex items-center bg-gradient-to-br from-primary/5 to-secondary/5">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex flex-col md:flex-row items-center">
                <div class="md:w-1/2 mb-10 md:mb-0">
                    <h1 class="text-[clamp(2rem,5vw,3.5rem)] font-bold leading-tight text-balance">
                        在<span class="text-primary">技术堆</span>里<span class="text-secondary">找</span>灵感<br>
                    </h1>
                    <p class="mt-6 text-lg text-gray-600 max-w-xl">
                        欢迎来到<%= config.title %>，这是我折腾代码与脑洞的小角落
                    </p>
                    <div class="mt-8">
                        <a href="#articles" class="px-6 py-3 bg-primary text-white rounded-lg font-medium hover:bg-primary/90 transition-all shadow-lg shadow-primary/20">
                            查看记录
                        </a>
                    </div>
                </div>
                <div class="md:w-1/2 flex justify-center">
                    <div class="relative">
                        <div class="absolute -top-4 -left-4 w-24 h-24 bg-primary/10 rounded-lg animate-float" style="animation-delay: 0s;"></div>
                        <div class="absolute -bottom-4 -right-4 w-32 h-32 bg-secondary/10 rounded-lg animate-float" style="animation-delay: 1s;"></div>
                        <img src="https://picsum.photos/600/400?random=1" alt="技术与创意的融合" class="w-full max-w-md rounded-xl shadow-2xl relative z-10">
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- 文章区域 -->
    <section id="articles" class="py-16 bg-white">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center mb-12">
                <h2 class="text-3xl font-bold">最新文章</h2>
                <a href="<%- url_for('/archives/') %>" class="text-primary font-medium hover:underline flex items-center">
                    查看全部 <i class="fa fa-arrow-right ml-2"></i>
                </a>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% page.posts.each(function(post) { %>
                <article class="bg-white rounded-xl overflow-hidden shadow-md card-hover">
                    <div class="relative h-48 overflow-hidden">
                        <% if (post.cover) { %>
                        <img src="<%= post.cover %>" alt="<%= post.title %>" class="w-full h-full object-cover">
                        <% } else { %>
                        <img src="https://picsum.photos/600/400?random=2" alt="<%= post.title %>" class="w-full h-full object-cover">
                        <% } %>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-bold mb-2">
                            <a href="<%- post.permalink %>"><%= post.title %></a>
                        </h3>
                        <% let text = post.content.replace(/<[^>]+>/g, '').trim(); %>
                        <% let isChinese = /[一-龥]/.test(text); %>
                        <% let length = isChinese ? 50 : 100; %>
                        <p class="text-gray-600 mb-4"><%= post.excerpt || text.substring(0, length) + (text.length > length ? '...' : '') %></p>
                        <div class="flex items-center">
                            <span class="text-sm text-gray-500">
                                <i class="fa fa-calendar mr-1"></i> <%= date(post.date, 'YYYY-MM-DD') %>
                            </span>
                        </div>
                    </div>
                </article>
                <% }); %>
            </div>
            <!-- 特色文章 -->
            <% site.posts.filter(post => post.featured).limit(1).each(function(post) { %>
            <div class="mt-16 mb-16 bg-gradient-to-r from-primary/10 to-secondary/10 rounded-2xl p-8 md:p-12">
                <div class="flex flex-col md:flex-row items-center">
                    <div class="md:w-2/3 mb-8 md:mb-0 md:pr-8">
                        <div class="flex items-center mb-4">
                            <div class="w-10 h-10 rounded-full bg-primary/20 flex items-center justify-center text-primary">
                                <i class="fa fa-star"></i>
                            </div>
                            <span class="ml-3 text-primary font-medium">特色文章</span>
                        </div>
                        <h3 class="text-2xl md:text-3xl font-bold mb-4">
                            <a href="<%- post.permalink %>" class="hover:text-primary transition-colors"><%= post.title %></a>
                        </h3>
                        <% let text = post.content.replace(/<[^>]+>/g, '').trim(); %>
                        <% let isChinese = /[一-龥]/.test(text); %>
                        <% let length = isChinese ? 100 : 150; %>
                        <p class="text-gray-600 mb-6 md:mb-8">
                            <%= post.excerpt || text.substring(0, length) + (text.length > length ? '...' : '') %>
                        </p>
                        <div class="flex items-center">
                            <img src="<%- url_for('/images/default-avatar.jpg') %>" class="w-10 h-10 rounded-full">
                            <div class="ml-3">
                                <div class="font-medium"><%= config.author.name %></div>
                            </div>
                        </div>
                    </div>
                    <div class="md:w-1/3">
                        <img src="<%- post.cover ? post.cover : `https://picsum.photos/600/400?random=${post.id}` %>" alt="<%= post.title %>" class="rounded-xl shadow-lg w-full">
                    </div>
                </div>
            </div>
            <% }); %>


    <!-- 分类区域 -->
    <section id="categories" class="py-16 bg-gray-50">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <h2 class="text-3xl font-bold mb-12">分类</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% site.categories.each(function(category) { %>
                <a href="<%- category.permalink %>" class="bg-white rounded-xl p-6 card-hover">
                    <h3 class="text-xl font-bold mb-2"><%= category.name %></h3>
                    <p class="text-gray-600"><%= category.posts.length %> 篇文章</p>
                </a>
                <% }); %>
            </div>
        </div>
    </section>

    <!-- 标签区域 -->
    <section id="tags" class="py-16 bg-white">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <h2 class="text-3xl font-bold mb-8">标签</h2>
            <div class="flex flex-wrap gap-3">
                <% site.tags.each(function(tag) { %>
                <a href="<%- tag.permalink %>" class="bg-primary/10 text-primary px-4 py-2 rounded-full text-sm hover:bg-primary/20 transition-colors">
                    <%= tag.name %>
                </a>
                <% }); %>
            </div>
        </div>
    </section>


    <script>
        // 移动端菜单切换
        document.getElementById('menuBtn').addEventListener('click', function() {
            document.getElementById('mobileMenu').classList.toggle('hidden');
        });

        // 导航栏滚动效果
        window.addEventListener('scroll', function() {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('bg-white', 'shadow-md');
            } else {
                navbar.classList.remove('bg-white', 'shadow-md');
            }
        });
    </script>
            <!-- 关于模块 -->
            <section id="about" class="py-16 bg-gradient-to-br from-primary/5 to-secondary/5">
                <div class="container mx-auto px-4 sm:px-6 lg:px-8">
                    <div class="text-center mb-12">
                        <h2 class="text-3xl font-bold">关于博主</h2>
                        <div class="w-20 h-1 bg-primary mx-auto mt-4"></div>
                    </div>
                    <div class="max-w-3xl mx-auto bg-white rounded-2xl shadow-lg overflow-hidden">
                        <div class="md:flex">
                            <div class="md:w-1/3 bg-primary/10 flex items-center justify-center p-8">
                                <% const author = config.author || {}; %>
                                <div class="text-center">
                                    <% if (author.avatar) { %>
                                        <img src="<%- url_for(author.avatar) %>" alt="<%= author.name %>" class="w-32 h-32 rounded-full mx-auto object-cover border-4 border-white shadow-lg">
                                    <% } else { %>
                                        <img src="<%- url_for('/images/default-avatar.jpg') %>" alt="<%= author.name || 'Author' %>" class="w-32 h-32 rounded-full mx-auto object-cover border-4 border-white shadow-lg">
                                    <% } %>
                                    <h3 class="mt-4 text-xl font-bold"><%= author.name || 'John Doe' %></h3>
                                    <p class="text-primary mt-1"><%= config.subtitle || 'Tech Enthusiast' %></p>
                                </div>
                            </div>
                            <div class="md:w-2/3 p-8 md:p-10">
                                <h4 class="text-xl font-semibold mb-4">关于我</h4>
                                <p class="text-gray-700 leading-relaxed mb-6"><%= config.description || '热爱技术与分享的开发者，致力于记录学习历程和技术心得。' %></p>
                                <div class="flex flex-wrap gap-4 mt-6">
                                    <a href="#" class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center text-primary hover:bg-primary hover:text-white transition-colors">
                                        <i class="fa fa-github"></i>
                                    </a>
                                    <a href="#" class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center text-primary hover:bg-primary hover:text-white transition-colors">
                                        <i class="fa fa-twitter"></i>
                                    </a>
                                    <a href="#" class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center text-primary hover:bg-primary hover:text-white transition-colors">
                                        <i class="fa fa-linkedin"></i>
                                    </a>
                                    <a href="#" class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center text-primary hover:bg-primary hover:text-white transition-colors">
                                        <i class="fa fa-envelope"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>

</body>
</html>